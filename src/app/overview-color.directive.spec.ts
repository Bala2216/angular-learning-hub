import { OverviewColorDirective } from './overview-color.directive';

describe('OverviewColorDirective', () => {
  it('should create an instance', () => {
    const directive = new OverviewColorDirective();
    expect(directive).toBeTruthy();
  });
});
